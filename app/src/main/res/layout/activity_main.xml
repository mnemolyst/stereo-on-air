<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/camera_button"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:text="Camera"
        app:layout_constraintLeft_toLeftOf="parent" />

    <Button
        android:id="@+id/stop_camera_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Stop Camera"
        app:layout_constraintLeft_toRightOf="@id/camera_button" />

    <Button
        android:id="@+id/receiver_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Receiver"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@id/camera_button"/>

    <Button
        android:id="@+id/stop_receiver_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Stop Receiver"
        app:layout_constraintLeft_toRightOf="@id/receiver_button"
        app:layout_constraintTop_toBottomOf="@id/camera_button"/>

    <TextView
        android:id="@+id/peer_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="peers"
        app:layout_constraintTop_toBottomOf="@id/receiver_button"
        app:layout_constraintLeft_toLeftOf="parent" />

    <TextView
        android:id="@+id/connection_textview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="connection info"
        app:layout_constraintTop_toBottomOf="@id/peer_textview"
        app:layout_constraintLeft_toLeftOf="parent" />

    <SurfaceView
        android:id="@+id/surface_view"
        android:layout_width="160dp"
        android:layout_height="120dp"
        app:layout_constraintTop_toBottomOf="@id/connection_textview"
        app:layout_constraintBottom_toBottomOf="parent" />
</android.support.constraint.ConstraintLayout>